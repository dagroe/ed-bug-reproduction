<div>
  Number of items: {{@model.items.length}}
</div>
{{#each @model.items as |item|}}
  <div data-test-item>
    {{item.name}} <button type="button" {{on "click" (fn this.deleteItem item)}} data-test-delete-button>delete</button>
  </div>
{{/each}}